AnnouncementService — это учебный pet-проект, представляющий собой полноценное мини-приложение для создания и отображения объявлений. Проект демонстрирует подход к построению полного цикла web-системы: от backend-сервиса и frontend-клиента до контейнеризации и запуска через Docker.

🎯 Назначение проекта

Сервис позволяет:

Создавать и хранить объявления

Просматривать список объявлений

Отображать отдельные объявления

Демонстрировать работу пользовательского интерфейса + REST API

Показать навыки разработки full-stack приложения, организации структуры проекта и настройки инфраструктуры

Проект не претендует на production-готовность — это тренировка архитектуры, Docker-окружения и развёртывания.

🏗 Архитектура

Проект состоит из трёх основных компонентов:

1. Backend (Java, Spring Boot)

REST API для работы с объявлениями

Логика создания, получения и хранения данных

Работа с БД / хранилищем (реализация зависит от текущей версии проекта)

Сервис запускается в отдельном контейнере

2. Frontend (TypeScript / SPA)

Приложение отображает список объявлений

Позволяет просматривать отдельные записи

Общается с backend-сервисом через REST API

Сервируется через Nginx

3. Инфраструктура
Nginx

Reverse-proxy

Раздача frontend

Проксирование API-запросов на backend

Docker / docker-compose

Один командой поднимаются все сервисы

Конфигурационные файлы находятся в infra/nginx и корне проекта

🧰 Стек технологий
Backend:

Java

Spring Boot

Gradle / Maven (в зависимости от конфигурации репозитория)

Frontend:

TypeScript

HTML/CSS

Fetch API / Axios (зависит от реализации)

Инфраструктура:

Docker

Docker Compose

Nginx

🚀 Запуск через Docker

Убедитесь, что у вас установлены:

Docker

Docker Compose

1. Клонируйте репозиторий
git clone https://github.com/FedorovVladislav/announcementService.git
cd announcementService

2. Соберите и запустите проект
docker-compose up --build


Команда:

соберёт backend

соберёт frontend

поднимет Nginx

создаст все необходимые контейнеры

🌐 Доступ к сервисам

После запуска Docker приложение будет доступно по следующим адресам:

Frontend

👉 http://localhost:80

или просто
👉 http://localhost

Здесь отображается пользовательский интерфейс.

Backend API

👉 http://localhost:8080/api

(порт может отличаться, если изменён в docker-compose.yml)

📁 Структура проекта (кратко)
announcementService/
├── frontend/         # SPA-клиент
├── services/         # backend-сервис на Java
├── infra/
│   └── nginx/        # конфигурация Nginx
├── docker-compose.yml
└── README.md

📌 Планы / Roadmap

Добавить авторизацию

Добавить создание объявлений из UI

Подключить реальную БД

Улучшить документацию API (например, через Swagger)

Написать тесты для backend

Если хочешь — могу также создать архитектурную диаграмму, Swagger-доки, или дополнить README разделами «API Reference», «Скриншоты», «Demo».